server:
  port: 8090
  servlet:
    context-path: /bp-business

spring:
  application:
    name: bp-business
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.3.75:8848
    sentinel:
      transport:
        # sentinel控制台的地址
        dashboard: 192.168.3.75:8080
      # 立即加载
      eager: true